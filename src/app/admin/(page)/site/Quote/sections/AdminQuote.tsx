import { Box } from '@mantine/core';
import { AdminQuoteList } from '@/app/admin/(page)/site/Quote/components/AdminQuote.List';

export const AdminQuote = () => {
  return (
    <Box>
      <AdminQuoteList />
    </Box>
  );
};
